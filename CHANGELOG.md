# 更新日志 (Changelog)

## v2.0.0 (2026-08-20)

### 新增
- **替换优先级优化**：执行替换时自动按查找词长度从长到短排序，字数多的规则优先替换
  - 例：同时配置 `xiejing→泄精` 与 `xie→邪`，输入 `xiejing` 现正确输出 `泄精`，不再被短词先截胡成 `邪jing`
- **输入框「粘贴」按钮**：一键把剪贴板内容填入输入框

### UI 调整
- 输入区按钮重新布局：`📂 本地` ｜ `📋 粘贴` ｜ `🧹 清空`（原「从文件读取 / 清空输入」）
- 结果区按钮 `清空结果` 简化为 `清空`
- 输入区标题 `输入文本（粘贴或编辑）` 简化为 `输入文本`
- 库管理按钮 `重命名` 改为 `改名`，与「新建库 / 删库」长度比例协调，不再换行
- 新建库输入框提示文字改为中性的「给这个库起个名字」

### 说明
- 版本号升级至 2.0.0（versionCode 2）

## v1.0.0 (2026-08-19)
首个发布版本。

### 新增
- 多替换库（多配置）：可新建任意多个独立命名、独立内容的规则库
- 批量替换：每个库支持任意多条规则（查找词 → 替换词），非单次替换
- 粘贴 / 从文件读取（自动按所选编码解码）
- 编码选择：UTF-8 / GBK / GB2312 / Big5 / UTF-16LE / ASCII
- 输入框、结果框独立滚动，支持长按选中 / 全选 / 删除
- 「清空输入」「清空结果」一键清空按钮
- 保存自由选路径（系统文件选择器，默认 Download，无需 root / 存储权限）
- 一键复制替换结果
- 纯本地运行，不联网

### 修复
- 修复 GBK / GB2312 / Big5 编码支持（引入 ICU4J，安卓原生不支持这些编码）
- 移除弃用的 manifest `package` 属性
- 初始启动不再预设任何替换库名和规则，全部由用户自行添加

### 说明
- APK 使用 debug 签名构建（v2 方案），可直接安装；如需上架商店需配置正式签名
